# Cargar compinit antes de fzf-tab
autoload -Uz compinit && compinit

# Cargar fzf-tab
zinit light Aloxaf/fzf-tab

# Configurar el estilo del menú
zstyle ':completion:*' menu select
zstyle ':completion:*' matcher-list 'm:{a-zA-Z}={A-Za-z}' 'r:|=*' 'l:|=* r:|=*'